语音转写的播客笔记里,下面这个词可能是专有名词转写错误(发音相近、写法不同)。你的任务分两步:先猜,再设计搜索验证。

节目: {{podcast}}
可疑词: {{term}}
上下文: {{context}}

第一步(在心里完成):结合上下文猜测这个词的真实身份——有哪些「发音与可疑词相近、且在这个语境里说得通」的真实专有名词?比如上下文在聊 AI 播客,"No Players" 读音接近的真实播客是 "No Priors"。用你自己的知识大胆假设 1-2 个候选。

第二步(输出):设计恰好 2 条搜索查询去验证:
- 第 1 条:验证你最可能的候选(直接搜候选词 + 语境关键词,确认它真实存在且与语境吻合)
- 第 2 条:兜底——若没把握,用「可疑词 + 上下文关键词」或「上下文并列实体 + 领域名单」类查询,让正确写法出现在结果里

只输出一个 JSON 字符串数组(恰好 2 条查询),不要 Markdown 代码块,不要任何说明文字。
