{
  "name": "podnote",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "小宇宙播客 → 本地转写 → AI 结构化笔记 (Pi agent 底座)",
  "scripts": {
    "note": "node src/index.mjs"
  },
  "dependencies": {
    "@earendil-works/pi-agent-core": "latest",
    "@earendil-works/pi-ai": "latest"
  },
  "engines": {
    "node": ">=20"
  }
}
